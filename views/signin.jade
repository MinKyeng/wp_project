extends layout

block content
  .container#sign
    h1.page-header 로그인
    form.form-signin(action='/signin', method='POST')
      //- a.btn.btn-facebook.btn-lg.btn-block.mb20(href='/auth/facebook')
      //-   span.icon
      //-     i.fa.fa-facebook.fa-lg
      //-   span 페이스북 계정으로 로그인          

      //- .tc.mb20
      //-   h6 또는
      .input-group
        label(for='email', class='sr-only') Email address
        input(
          type='email', id='email', name='email', class='form-control', 
          placeholder='Email address', required, autofocus, aria-describedby="email"
        )
        span.input-group-addon
          i.fa.fa-envelope-o#email

      .input-group
        label(for='password', class='sr-only') Password
        input(
          type='password', id='password', name='password', class='form-control', 
          placeholder='Password', required, autofocus, aria-describedby="pwd"
        )
        span.input-group-addon
          i.fa.fa-lock#pwd
          
      button.btn.btn-lg.btn-pink.btn-block.mb30(type='submit') 로그인
      hr

      .clearfix.mt30
        .pull-left 계정이 없으세요?
        .pull-right
          a.btn.btn-new.btn-mini(href='/users/new') 회원가입
      | 
      //-a.btn.btn-default.btn-mini(href='/reset-password') 비밀번호 재설정
      
