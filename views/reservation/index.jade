extends ../layout

block content
  .container
    h1.page-header 예약목록

    table.table.table-bordered
      thead
        tr
          th 작성자
          th 체크인
          th 체크아웃
          th 작성일
      tbody
        each reservation in reservations
          tr
            td
              a(href='/reservation/#{reservation.id}')= reservation.name
            td= moment(reservation.checkin).format('YYYY-MM-DD')
            td= moment(reservation.checkin).format('YYYY-MM-DD')
            td= moment(reservation.createdAt).format('YYYY-MM-DD HH:mm:ss')

    //-a.btn.btn-primary(href='/reservation/new') 글쓰기

    
